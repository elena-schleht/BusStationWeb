@page
@model BusStationWeb.Pages.Admin.Vokzals.DeleteModel

@{
    ViewData["Title"] = "Удалить";
}

@if (Model.TypeModel == "citie")
{
    <h4>Вы уверены, что хотите удалить город?</h4>
    <div>
        <h3>Город</h3>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                Наименовние
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Citie.NameCity)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Citie.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" asp-route-id="@Model.Citie.Id" asp-route-type="citie" /> |
            <a asp-page="./Index">Вернуться</a>
        </form>
    </div>
}
